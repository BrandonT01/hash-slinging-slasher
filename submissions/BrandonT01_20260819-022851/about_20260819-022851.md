# Submission 20260819-022851

- from: BrandonT01
- names: 273027
- dropped as already published: 0
- runs included: 3
- searched: every pool
- expected coincidental matches: 0.000000

Every name here was confirmed against the game's own loaded assets, and checked against the community tables immediately before sending.

## How these were found

### run_20260819-020637_streamkeys
- method: stream-key grammar sweep (streamkeys)
- what it does: the streamkey pool is generated sentences, not names -- level paths, a bounded terrain quadtree, material-group lines with spaces in them, and the exact rule that a sound `x.snd` has a stream key `x.snd_transient`. Each family is swept to its measured edge rather than sampled
- ran for: 21s

### run_20260819-020906_streamkeys
- method: stream-key grammar sweep (streamkeys)
- what it does: the streamkey pool is generated sentences, not names -- level paths, a bounded terrain quadtree, material-group lines with spaces in them, and the exact rule that a sound `x.snd` has a stream key `x.snd_transient`. Each family is swept to its measured edge rather than sampled
- ran for: 21s

### run_20260819-022157_streamkeys
- method: stream-key grammar sweep (streamkeys)
- what it does: the streamkey pool is generated sentences, not names -- level paths, a bounded terrain quadtree, material-group lines with spaces in them, and the exact rule that a sound `x.snd` has a stream key `x.snd_transient`. Each family is swept to its measured edge rather than sampled
- ran for: 22s
