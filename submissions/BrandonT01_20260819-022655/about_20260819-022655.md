# Submission 20260819-022655

- from: BrandonT01
- names: 297440
- dropped as already published: 0
- runs included: 6
- searched: 5 pool(s): image, material, sound_asset, xanim, xmodel
- expected coincidental matches: 0.000000

Every name here was confirmed against the game's own loaded assets, and checked against the community tables immediately before sending.

## How these were found

### run_20260819-014621_streamkeys
- method: stream-key grammar sweep (streamkeys)
- what it does: the streamkey pool is generated sentences, not names -- level paths, a bounded terrain quadtree, material-group lines with spaces in them, and the exact rule that a sound `x.snd` has a stream key `x.snd_transient`. Each family is swept to its measured edge rather than sampled
- ran for: 9s

### run_20260819-020023_streamkeys
- method: stream-key grammar sweep (streamkeys)
- what it does: the streamkey pool is generated sentences, not names -- level paths, a bounded terrain quadtree, material-group lines with spaces in them, and the exact rule that a sound `x.snd` has a stream key `x.snd_transient`. Each family is swept to its measured edge rather than sampled
- ran for: 22s

### run_20260819-020433_streamtree
- method: stream-tree zone peel (streamtree)
- what it does: peels the exact tail ` stream tree mtlGroup <N>` off every unnamed stream key, leaving the hash the zone name must have, then answers all of them in one pass over candidate zones -- the sum of the two lists rather than the product
- ran for: 1m 17s

### run_20260819-021206_streamkeys
- method: stream-key grammar sweep (streamkeys)
- what it does: the streamkey pool is generated sentences, not names -- level paths, a bounded terrain quadtree, material-group lines with spaces in them, and the exact rule that a sound `x.snd` has a stream key `x.snd_transient`. Each family is swept to its measured edge rather than sampled
- ran for: 22s

### run_20260819-022132_streamkeys
- method: stream-key grammar sweep (streamkeys)
- what it does: the streamkey pool is generated sentences, not names -- level paths, a bounded terrain quadtree, material-group lines with spaces in them, and the exact rule that a sound `x.snd` has a stream key `x.snd_transient`. Each family is swept to its measured edge rather than sampled
- ran for: 24s

### run_20260819-022500_general_recovered
- method: general search (confirm_cw), interrupted
- what it does: beginning + stem + ending over every seed, peeling endings off the wanted ids; this pass was stopped at roughly a third of its way through, so these are its checkpointed names recovered into a run folder of their own
- ran for: 47m
